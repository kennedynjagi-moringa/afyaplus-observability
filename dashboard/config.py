"""Dashboard-wide configuration: budget caps used by the Budget Capital
Utilisation section. Set to roughly 20% headroom above the Phase 3
30-day projection so utilisation reads as "healthy but worth watching",
matching the current spend level rather than an arbitrary number."""

DAILY_BUDGET_CAP_USD = 1.50
MONTHLY_BUDGET_CAP_USD = 35.00
